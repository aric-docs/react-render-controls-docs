{
  "name": "react-render-controls-docs",
  "version": "0.0.1",
  "description": "react-render-controls docs.",
  "scripts": {
    "start": "npm run dev",
    "dev": "dumi dev",
    "build": "dumi build",
    "postbuild": "workbox generateSW workbox-config.cjs",
    "preview": "dumi preview",
    "prepare": "husky install && dumi setup"
  },
  "authors": [
    "1290657123@qq.com"
  ],
  "license": "MIT",
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{md,json}": [
      "prettier --write --no-error-on-unmatched-pattern"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "dumi": "^2.4.13",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "prettier": "^2.7.1",
    "workbox-cli": "^7.0.0"
  },
  "dependencies": {
    "@jswork/react-render-controls": "^1.2.1"
  }
}
